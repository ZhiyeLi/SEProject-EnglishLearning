src/
├── components/          # 组件库根目录
│   ├── common/          # 通用基础组件（跨业务、全局复用）
│   │   ├── NavBar.vue   # 顶部导航栏（所有页面通用）
│   │   ├── EndBar.vue   # 页脚（所有页面通用）
│   │   └── CustomButton.vue # 淡绿色主题按钮（全局操作按钮）
│   ├── business/        # 业务专属组件（英语学习场景绑定）
│   │   ├── FriendItem.vue # 好友项（好友列表场景）
│   ├── styles/          # 组件库样式资源
│   │   ├── _variables.css # 全局样式变量（主题色、尺寸）
│   └── index.js         # 组件库入口文件（批量导出、全局注册）
├── tailwind.config.js   # 主题定制（组件库统一样式配置）
└── docs/                # 组件库文档（可选，便于团队协作）
    ├── 组件列表.md      # 组件清单、用途说明
    └── 使用示例.md      # 组件调用代码示例